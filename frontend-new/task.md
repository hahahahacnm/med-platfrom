# 前端界面重构任务追踪卡

## 🏆 总体任务与愿景
**需求**: 重构所有前端，打造出色的现代美观、动效优雅、排版合理的用户界面，并必须采用 `TailwindCSS + TypeScript + React + Astro` 这个极具前瞻性的技术栈。
**方向**: 放弃原有 `Vue 3 + Naive UI` 的实现，从零在 `frontend-new` 中应用一套更加定制化且无包袱的设计语言。

## 📋 模块执行进度

### 1. 技术栈脚手架 (Stack Setup)
- [x] Astro 初始化启动。
- [x] React & Tailwind 插件集成并安装。
- [x] 工具集安装 (`lucide-react`, `framer-motion`, `clsx`, `tailwind-merge`)。

### 2. 基础组件库 (UI Infrastructure)
- [x] 调色板与全局 CSS (`src/styles/global.css`) - 标准化的 HSL 色彩空间。
- [x] 通用工具 (`src/lib/utils.ts`) - `twMerge` + `clsx` 保障类名合并无冲突。
- [x] 侧边栏与头部导航 (`Sidebar.tsx`, `Topbar.tsx`) - 支持动态选中的 Framer Motion 背景交互、自适应移动端、精美图标。
- [x] Astro 顶层布局外壳 (`Layout.astro`)。

### 3. 主干页面 (Pages & Views)
- [x] 仪表盘首页 (`pages/index.astro`, `DashboardContent.tsx`) - 渐变 Banner，数字进度概览，交互式热力图模拟。
- [x] 题库中心 (`pages/quiz.astro`, `QuizContent.tsx`) - 清新的各科复习卡片布局，带直观进度条与图标设计。
- [ ] 错题本 (`pages/mistakes.astro`)。
- [ ] 收藏夹 (`pages/favorites.astro`)。
- [ ] 笔记本 (`pages/notes.astro`)。
- [ ] 个人资料中心 (`pages/profile.astro`)。

## 📝 备注与反馈
1. React 在 Astro 中通过 `client:load` 或者 `client:idle` 指令被激活，请注意组件交互正常。
2. 保持一切代码注释和文档强制使用**中文**。
3. 当前依赖已经在后台 `npm install`。
