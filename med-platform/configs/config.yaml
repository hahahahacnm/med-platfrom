app:
  name: med-platform
  env: dev
  port: 8080

log:
  level: debug

data:
  database:
    driver: postgres
    source: "host=localhost user=postgres password=243658cjh dbname=med_platform port=5432 sslmode=disable TimeZone=Asia/Shanghai"

jwt:
  secret: "243658cjh" # 生产环境这个要很复杂
  expire: 24 # 过期时间 (小时)

# 💰 支付配置
payment:
  # 当前模式: "dev" (开发模式, 允许使用 mock 渠道), "prod" (生产模式, 禁用 mock)
  mode: "dev" 
  
  # 域名配置 (用于支付回调)
  # 开发时填内网穿透地址(如 http://xxx.cpolar.cn)，上线填真实域名(https://api.med.com)
  domain: "http://localhost:8080" 

  # 🔷 支付宝配置 (预留)
  alipay:
    app_id: ""
    private_key: ""
    public_key: ""
    
  # 🟩 微信支付配置 (预留)
  wechat:
    app_id: ""      # 公众号/小程序ID
    mch_id: ""      # 商户号
    api_v3_key: ""  # APIv3 密钥
    cert_serial: "" # 证书序列号
    private_key: "" # 商户私钥